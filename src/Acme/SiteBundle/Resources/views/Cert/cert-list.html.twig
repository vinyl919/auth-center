{% extends 'AcmeSiteBundle:Default:base.html.twig' %}
{% block body %}
	<div class="panel panel-default">
		<div class="panel-heading">Lista podpisanych certyfikatów</div>
		<table class="table">
			<tr>
				<th>Id</th>
				<th>Nazwa</th>
				<th>Data utworzenia</th>
				<th>Akcja</th>
			</tr>
		{% for cert in data %}
			<tr>
				<td>{{ cert.id }}</td>
				<td>{{ cert.name }}</td>
				<td>{{ cert.date|date('Y-m-d') }}</td>
				<td><button class="btn btn-default multi-btn">Pobierz</button><button class="btn btn-default multi-btn">Skasuj</button></td>
			</tr>
		{% endfor %}
		</table>
	</div>
{% endblock %}